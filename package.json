{
  "name": "ise-placement",
  "version": "1.0.0",
  "scripts": {
    "install-all": "cd backend && npm install --ignore-scripts && cd ../frontend && npm install --ignore-scripts && node node_modules/esbuild/install.js",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node src/server.js"
  }
}
